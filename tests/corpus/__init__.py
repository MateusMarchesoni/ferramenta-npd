"""Corpus de cotações-exemplo: a amostra que a mesa não tem.

Ver `casos.py` para o catálogo e `gerar.py` para transformar cada caso em
arquivo. `medir.py` roda o corpus inteiro contra `ler_cotacao` e imprime o
acerto por campo.
"""
